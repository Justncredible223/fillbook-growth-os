<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Growth OS's own mark, not Fillbook's: the ring gauge motif from
         Radar's ScoreBadge (the app's real visual signature) in the
         actual brand cyan, on the app's own dark background, a distinct
         silhouette from Fillbook's flat cyan square with bars logo so the
         two are never confused on a home screen, while still reading as
         the same product family (same cyan, same growth progress idea). -->
    <path
        android:pathData="M54,26 A28,28 0 1,1 26,54"
        android:strokeColor="#0891B2"
        android:strokeWidth="9"
        android:strokeLineCap="round"
        android:fillColor="#00000000" />
    <path
        android:pathData="M54,26 A28,28 0 0,1 72,33"
        android:strokeColor="#22B8DC"
        android:strokeWidth="9"
        android:strokeLineCap="round"
        android:fillColor="#00000000" />
</vector>
